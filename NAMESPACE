export(NBDev, PoisDev, QL.fit, QL.results, glmsolve, nlsolve)
#export(duplicated.matrix, unique.matrix)

import(edgeR)

importFrom(mgcv, negbin)
importFrom(pracma, broyden)

import(uniqueAtomMat)

#importFrom(varComp, informationTypes)
#import(MASS)
#importFrom(nleqslv, nleqslv)



S3method(coef, glm)
#S3method(unique, matrix)
#S3method(duplicated, matrix)

useDynLib(QuasiSeq, .registration =TRUE, .fixes="C_")
